/*const User


exports.postUser = (req, res) => {
    Todo.create(req.body)
        .then((data) => res.json({
            message: "login added",
            data
        }))
        .catch((err) =>
            res
            .status(400)
            .json({
                message: "Failed to add User",
                error: err.message
            })
        );
};*/